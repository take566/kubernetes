apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vllm-kind
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/take566/kubernetes.git
    targetRevision: master
    path: vllm/overlays/kind
  destination:
    server: https://kubernetes.default.svc
    namespace: vllm
  syncPolicy:
    # Manual sync — kind has no GPU; avoid auto-deploying GPU workloads in dev/CI
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
