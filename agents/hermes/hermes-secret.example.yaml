# WARNING: REPLACE_ME placeholder. NEVER commit real keys.
# Copy this file to hermes-secret.yaml (gitignored) and replace values, then apply manually:
#   kubectl apply -f agents/hermes/hermes-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: hermes-secret
  namespace: agents
  labels:
    app: hermes
    app.kubernetes.io/name: hermes
    app.kubernetes.io/part-of: agents
    app.kubernetes.io/managed-by: kustomize
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  API_SERVER_KEY: "REPLACE_ME"
