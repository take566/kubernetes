apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - local-path-storage
  - metrics-server
  # GPU plugins — apply only on clusters with matching hardware:
  # - nvidia-device-plugin
  # - see amd-gpu-device-plugin/README.md
