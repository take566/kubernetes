apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NVIDIA inference base (no PV). Deploy only via overlays/kubeadm or overlays/kind.
namespace: vllm
labels:
  - pairs:
      app.kubernetes.io/part-of: vllm
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false
    includeTemplates: false
resources:
  - namespace.yaml
  - vllm-configmap.yaml
  - vllm-deployment.yaml
  - vllm-service.yaml
