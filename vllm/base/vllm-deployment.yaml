apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm
  namespace: vllm
  labels:
    app: vllm
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: vllm
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 1
  # GPU-backed inference servers are typically single-replica
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: vllm
  template:
    metadata:
      labels:
        app: vllm
        app.kubernetes.io/name: vllm
        app.kubernetes.io/part-of: vllm
        app.kubernetes.io/managed-by: kustomize
    spec:
      # Uncomment when GPU nodes are tainted, e.g. key=nvidia.com/gpu
      # tolerations:
      #   - key: nvidia.com/gpu
      #     operator: Exists
      #     effect: NoSchedule
      # Uncomment when GPU nodes carry a dedicated label
      # nodeSelector:
      #   nvidia.com/gpu.present: "true"
      containers:
        - name: vllm
          image: vllm/vllm-openai:latest
          imagePullPolicy: Always
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              exec python3 -m vllm.entrypoints.openai.api_server \
                --model "${VLLM_MODEL}" \
                --host "${VLLM_HOST}" \
                --port "${VLLM_PORT}" \
                ${VLLM_EXTRA_ARGS}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: vllm-config
          env:
            # Optional: create vllm-secret.yaml from vllm-secret.example.yaml for gated models
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: vllm-secret
                  key: HF_TOKEN
                  optional: true
            - name: HUGGING_FACE_HUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: vllm-secret
                  key: HF_TOKEN
                  optional: true
            - name: VLLM_LOGGING_LEVEL
              value: "WARNING"
          resources:
            requests:
              cpu: "2"
              memory: 8Gi
              # Requires NVIDIA Device Plugin on GPU nodes.
              # Without a GPU-capable node, the Pod stays Pending.
              nvidia.com/gpu: "1"
            limits:
              cpu: "4"
              memory: 16Gi
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: model-cache
              mountPath: /data/huggingface
          # Startup (model download + load + compile) can take far longer than
          # the old liveness grace of 120s + 5x30s: on deploy-note (GTX 1650)
          # liveness killed the engine mid-start (exit 137, 13 restarts) before
          # it ever listened on :8000. startupProbe allows up to 30 minutes and
          # holds liveness / readiness off until the server answers once.
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 180
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 4
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 5
      volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: vllm-model-cache
