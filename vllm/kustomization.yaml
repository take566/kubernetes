apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Kustomize base (hostPath PV). Deploy via overlays/kubeadm or overlays/kind (see README.md).
namespace: vllm
labels:
  - pairs:
      app.kubernetes.io/part-of: vllm
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false
    includeTemplates: false
resources:
  - namespace.yaml
  - vllm-configmap.yaml
  - vllm-pv.yaml
  - vllm-deployment.yaml
  - vllm-service.yaml
