apiVersion: batch/v1
kind: CronJob
metadata:
  name: serena-export
  namespace: vllm
  labels:
    app: serena-export
    app.kubernetes.io/name: serena-export
    app.kubernetes.io/part-of: vllm
    app.kubernetes.io/managed-by: kustomize
spec:
  # Off-peak daily export (UTC). Manual trigger: kubectl create job --from=cronjob/serena-export
  schedule: "0 3 * * *"
  suspend: true
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      ttlSecondsAfterFinished: 86400
      template:
        metadata:
          labels:
            app: serena-export
            app.kubernetes.io/name: serena-export
            app.kubernetes.io/part-of: vllm
        spec:
          restartPolicy: Never
          containers:
            - name: export
              image: python:3.11-slim-bookworm
              imagePullPolicy: IfNotPresent
              command:
                - /bin/bash
                - -c
                - exec python /scripts/serena_export.py
              envFrom:
                - configMapRef:
                    name: serena-export-config
              resources:
                requests:
                  cpu: "100m"
                  memory: 128Mi
                limits:
                  cpu: "500m"
                  memory: 512Mi
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
                - name: dataset
                  mountPath: /data/dataset
          volumes:
            - name: scripts
              configMap:
                name: serena-export-script
                defaultMode: 0755
            - name: dataset
              persistentVolumeClaim:
                claimName: vllm-finetune-dataset
